# Frontend
VITE_API_URL=http://localhost:8000/api/v1

# Backend runtime
APP_ENV=development
APP_URL=http://localhost:5173
DEPLOYMENT_NAME=careos-local
DATABASE_URL=sqlite+aiosqlite:///./careos.db
POSTGRES_PASSWORD=local-only-change-me
SECRET_KEY=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ALLOWED_HOSTS=localhost,127.0.0.1,[::1],testserver
SSO_ALLOWED_REDIRECT_HOSTS=localhost,127.0.0.1,[::1],careos.example.com
LOG_LEVEL=INFO
METRICS_ENABLED=true

# Identity & provider setup
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=

# Private document storage. Keep S3 buckets private and use workload identity in production.
STORAGE_BACKEND=filesystem
S3_BUCKET=
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
STORAGE_PREFIX=careos

# AI / document integrations
AI_API_KEY=
AI_BASE_URL=

# Optional operational settings
BACKUP_BUCKET=
BACKUP_REGION=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
